// Dusk — Firebase configuration
//
// Replace the placeholder values below with your real Firebase project config.
// Get them from: https://console.firebase.google.com/
//   → Project Settings → General → Your apps → Web app → SDK setup & configuration
//
// While these still start with "YOUR_", the site runs in DEMO mode
// (local-only fake data, no real logins).

export const firebaseConfig = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_PROJECT.firebaseapp.com",
  projectId: "YOUR_PROJECT_ID",
  storageBucket: "YOUR_PROJECT.appspot.com",
  messagingSenderId: "YOUR_SENDER_ID",
  appId: "YOUR_APP_ID"
};
